// import winston from "winston";
// winston.remove(winston.transports.Console);

// import logger from "../src/helper/logging/logger";

// try {
//   // Disabling winston console.log in case of tests.
//   // this is a flaky way to do this.
//   logger.transports[2].silent = true;
// } catch (err) {}
